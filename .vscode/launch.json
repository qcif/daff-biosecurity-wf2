{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Run BLAST Parser",
            "program": "${workspaceFolder}/blast_parser/parse.py",
            "cwd": "${workspaceFolder}/blast_parser",
            "args": [
                "output.xml",
            ]
        }
    ]
}
